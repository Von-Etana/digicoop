{
  "name": "digicoop-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "npm run dev --workspace=apps/admin",
    "mobile": "npm start --workspace=apps/mobile",
    "api": "npm run dev --workspace=packages/api",
    "admin": "npm run dev --workspace=apps/admin",
    "test": "npm test --workspaces"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}